"use strict";
const titleElement = document.querySelector(".title");
const i = "albondigas";

titleElement.innerHTML = "Bienvenida";
titleElement.innerHTML = titleElement.innerHTML + " adalaber";
titleElement.innerHTML= titleElement.innerHTML + " tonta " + i;
